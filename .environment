export TOOLCHAIN_HOME="${TOOLCHAINS_HOME}/toolchain-aarch64_cortex-a53_gcc-5.2.0_musl-1.1.16"
export ARCH="aarch64"
CROSS_PREFIX="aarch64-openwrt-linux-"
export STAGING_DIR="${TOOLCHAIN_HOME}"
TOOLCHAIN_PATH="${TOOLCHAIN_HOME}/bin"
TOOLCHAIN_INCLUDE="${TOOLCHAIN_HOME}/include"
TOOLCHAIN_LIBRARY="${TOOLCHAIN_HOME}/lib"
export PATH="${TOOLCHAIN_PATH}":$PATH
BUILD="x86_64-pc-linux-gnu"
HOST="x86_64-pc-linux-gnu"
TARGET="aarch64-openwrt-linux-musl"
SYSROOT="${TOOLCHAIN_HOME}"
export CC=${CROSS_PREFIX}gcc
export AS=${CROSS_PREFIX}as
export LD=${CROSS_PREFIX}ld
export AR=${CROSS_PREFIX}ar
export STRIP=${CROSS_PREFIX}strip
export NM=${CROSS_PREFIX}nm
export SIZE=${CROSS_PREFIX}size
export GDB=${CROSS_PREFIX}gdb
export CFLAGS="--sysroot $SYSROOT"
